package com.desertadventure.config;

/** Linear card-run tuning (hub MVP). */
public final class RunConfig {
    private RunConfig() {
    }

    /** HP restored after a non-boss stage victory before returning to hub (0 = disabled). */
    public static final float STAGE_VICTORY_HEAL = 6f;
}
